# 设计系统

## 设计原则

1. **图表优先** - 用户打开页面第一眼看到图表
2. **信息分层** - 核心数据突出，详情次要
3. **暗色主题** - 专业金融风格
4. **简洁克制** - 不堆砌元素，留白呼吸

## 颜色系统

### 背景色
```css
--bg-primary: #0a0a0a;      /* 页面背景 */
--bg-secondary: #141414;    /* 卡片背景 */
--bg-tertiary: #1f1f1f;     /* 输入框/次级背景 */
```

### 文字色
```css
--text-primary: #fafafa;    /* 主文字 */
--text-secondary: #a1a1aa;  /* 次级文字 */
--text-tertiary: #71717a;   /* 提示文字 */
```

### 功能色
```css
--accent: #22c55e;          /* 绿色 - 积极/Risk-On */
--danger: #ef4444;          /* 红色 - 警告/Risk-Off */
--warning: #f59e0b;         /* 橙色 - 中性/注意 */
--blue: #3b82f6;            /* 蓝色 - 主指标线 */
```

### 指标专用色
```css
--btc-orange: #f7931a;      /* BTC 相关 */
--rates-cyan: #06b6d4;      /* Rates 桶 */
--liq-green: #22c55e;       /* Liquidity 桶 */
--risk-amber: #f59e0b;      /* Risk 桶 */
```

## 字体

```css
font-family: 'Inter', 'Noto Sans SC', system-ui, sans-serif;
```

### 数字/代码
```css
font-family: 'JetBrains Mono', monospace;
```

### 字号规范
| 用途 | 大小 | 字重 |
|------|------|------|
| 页面标题 | 28px | 700 |
| 卡片数值（大） | 24px | 700 |
| 卡片数值（中） | 20px | 700 |
| 卡片数值（小） | 16px | 600 |
| 正文 | 14px | 400 |
| 标签/提示 | 12px | 500 |
| 小字 | 11px | 400 |

## 间距系统

基于 4px 网格：
- 4px, 8px, 12px, 16px, 20px, 24px, 32px, 48px

### 常用间距
| 场景 | 间距 |
|------|------|
| 卡片内边距 | 16px (小卡片 12px) |
| 卡片间距 | 12px |
| 区块间距 | 24px |
| 页面边距 | 24px (移动端 16px) |

## 圆角

```css
--radius-sm: 6px;   /* 按钮、标签 */
--radius-md: 12px;  /* 卡片、容器 */
--radius-lg: 16px;  /* 大容器 */
```

## 卡片样式

### 统计卡片 (.stat-card)
```css
.stat-card {
  background: var(--bg-secondary);
  border-radius: 12px;
  padding: 16px;
}
```

### 卡片内结构
```
┌─────────────────┐
│ LABEL          │  <- 11-12px, 次级色, 大写
│ VALUE          │  <- 20-24px, 主色, 粗体
│ hint/bar       │  <- 可选：提示文字或进度条
└─────────────────┘
```

## 状态指示

### 进度条 (.bucket-bar)
```css
.bucket-bar {
  height: 4px;
  background: var(--bg-tertiary);
  border-radius: 2px;
}
.bucket-fill.positive { background: var(--accent); }
.bucket-fill.negative { background: var(--danger); }
```

### 状态徽章
- Risk-On: 绿色背景 + 🟢
- Neutral: 灰色背景 + 🟡
- Risk-Off: 红色背景 + 🔴

## 图表规范

### 时间轴显示规则
| 缩放级别 | 显示格式 | 示例 |
|----------|----------|------|
| 全局（多年） | 仅年份 | 2020, 2021, 2022 |
| 中等（年内） | 仅月份 | 1, 2, 3... 或 Jan, Feb |
| 精细（月内） | 日期 | 1, 15, 30 |

**原则：** 缩小显示范围时，年份之间显示月份，避免重复显示年份。

### 容器
```css
.chart-container {
  background: var(--bg-secondary);
  border-radius: 12px;
  padding: 16px;
}
```

### 图表高度
- 桌面端: 400px
- 移动端: 300px

### 图例位置
- 放在图表标题右侧
- 可点击切换显示/隐藏

### 阈值线
- 使用虚线 (dashed)
- Risk-On 阈值: 绿色
- Risk-Off 阈值: 红色
- 零线: 灰色半透明

## 响应式断点

```css
@media (max-width: 768px) {
  /* 移动端适配 */
  .stats-grid { flex-direction: column; }
  .chart-wrapper { height: 300px; }
}
```
