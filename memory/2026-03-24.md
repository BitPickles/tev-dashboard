# 2026-03-24 工作日志

## Session 清理 (05:00)

**清理结果:**
- 清理前总数: 119
- 清理后总数: 119
- 删除数量: 0
- 修复缺失: 8

**各 Agent 详情:**
| Agent | Before | After | Missing | Pruned |
|-------|--------|-------|---------|--------|
| main | 82 | 82 | 1 | 0 |
| engineer | 1 | 1 | 2 | 0 |
| researcher | 4 | 4 | 1 | 0 |
| operator | 14 | 14 | 1 | 0 |
| assistant | 2 | 2 | 0 | 0 |
| engineer2 | 2 | 2 | 1 | 0 |
| family-finance | 11 | 11 | 0 | 0 |
| designer | 2 | 2 | 1 | 0 |
| what-to-mine-operator | 1 | 1 | 1 | 0 |

**说明:** 所有 session 均保留，仅修复了 8 个缺失引用。无异常。

## 什么值得挖 - 持续任务与优化阶段复盘

- Boss 明确要求：停止 what-to-mine-operator 等非开发角色，只保留 `engineer`、`engineer2`、`designer` 三个执行 Agent。
- 持续任务编排的核心教训被确认：AI Agent 约 5 分钟可完成一个原子任务，计划不能按人类节奏估算；每个 Agent 要准备至少 8 天深度 backlog，做完立即派下一个，不能闲置。
- 之前持续任务曾出现严重空转：main heartbeat 实际是 1h 而不是 5m，且心跳模型用 Claude 时遇到 overloaded；后续修正方向为 heartbeat=5m，并将检查落盘、汇报解耦设为铁律。
- `task-orchestrator` Skill 已按这次复盘优化：加入启动 checklist、去重规则、8 天 backlog 规则、Agent 不得闲置规则。
- what-to-mine 原开发阶段代码在本地仓库累计达到 125 commits；原功能开发阶段已完成，后续切到“全站逐页自查优化”阶段。
- 全站逐页优化阶段的规则被确认：19 个页面逐页推进；每页 3 个 Agent 并行，各做 2 轮；不下明确需求，只让 Agent 自己回答“这个页面还有什么可优化”，第一轮修、第二轮查遗漏。
- 优化阶段后续被 Boss 主动叫停；所有持续任务都已停止，HEARTBEAT.md 已清空为无活跃持续任务，what-to-mine 汇报 cron 已禁用。

## what-to-mine 部署结论（Vercel）

- Boss 明确指出：what-to-mine 目前不是正式 main 站，实际依赖 dev/测试部署；我曾误把代码 push 到 `main`，这是一次错误操作。
- Vercel 的 Git 自动部署失败根因已明确：git author 为 `1311453837+BitPickles@users.noreply.github.com` / `BitPickles`，但 Vercel 项目属于 `zhengzhibtc-4273` 团队；Hobby team 不支持协作者，因此 Git 触发部署会被 Blocked。
- 我随后改为使用当前已登录的 Vercel 身份 `zhengzhibtc-4273` 通过 CLI 直接部署，已绕开 Git author 限制。
- 但即使以 Boss 的 Vercel 身份执行 `vercel deploy` / `vercel deploy --prebuilt --prod` / preview prebuilt，部署最终仍然失败：deployment 状态为 `ERROR`，而内部 `builds[0].readyState = READY`，说明本地构建和上传成功，但 Vercel 在最终部署收尾阶段报错。
- 最终对 Boss 的准确结论：**部署没有成功**；本地 `npm run build` 成功，但线上 deployment 未 ready。
- Boss 对 Vercel 平台非常不满，并明确表达可以考虑放弃 Vercel；可选方向已讨论为 VPS 自部署或 Cloudflare Pages。
